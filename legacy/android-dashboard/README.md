# legacy/android-dashboard

本目录原计划归档项目早期的 Kotlin/Compose 仪表盘工程（LLM 配额 + 行情看板）。

在 2026-09-07 搭建 Rust workspace 时，该工程的源文件（app/、gradle/、gradlew、
settings.gradle.kts、build.gradle.kts、gradle.properties 等）已从工作区消失
（根目录、本目录与回收站中均不存在），无法归档保留。

当前仓库以 Rust 实现为主体，见根目录 README.md 与 docs/。
